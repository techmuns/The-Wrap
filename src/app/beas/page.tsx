import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function BeasPage() {
  return (
    <PlaceholderPage
      title="Beas"
      subtitle="Section to be defined."
      description="This section appears in the reference navigation but its purpose isn't defined yet, and we haven't identified matching content or a data source. It's stubbed here so the navigation is complete — we'll build it out once its intent is confirmed."
      planned={["Awaiting a definition of what this section should be"]}
    />
  );
}
